# Contributing

Keep the tool dependency-free and focused on separate Codex profiles. Add tests with synthetic paths and mocked processes for behavior changes. Run `python -m unittest discover -s tests -v`. Never include real profile data, credentials, account identifiers, or machine-specific paths in fixtures or issues. Discuss changes to the isolation mechanism with evidence from current Codex builds; Electron internals may change.
